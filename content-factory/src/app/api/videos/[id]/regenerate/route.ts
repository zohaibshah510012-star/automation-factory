import { NextResponse } from "next/server";
import { createVideoTask, getVideoTask, runVideoTask } from "@/lib/video-service";
import { requireUser } from "@/lib/request-auth";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const user = await requireUser(request); const original = await getVideoTask((await params).id, user.id); if (!original) return NextResponse.json({ error: "Video task not found" }, { status: 404 }); const task = await createVideoTask({ userId: user.id, prompt: original.prompt, model: original.model ?? undefined, durationSeconds: original.durationSeconds ?? undefined }); void runVideoTask(task.id); return NextResponse.json({ task }, { status: 201 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to regenerate video" }, { status: 400 }); } }
