import {NextResponse}from"next/server";import{requireAdmin}from"@/lib/request-auth";import{getAiProviders,getActiveProviderName}from"@/lib/ai-providers";import{getSupabaseServerClient}from"@/lib/supabase/server";
export async function POST(r:Request){try{await requireAdmin(r);const provider=getActiveProviderName();await getAiProviders().text.generateContentPack({topic:"health check",systemPrompt:"Return JSON only: title, script, storyboard with exactly four strings.",userPrompt:"Return a short health check content pack."});await getSupabaseServerClient()!.from("system_logs").insert({level:"info",event:"provider_test_connected",metadata:{provider}});return NextResponse.json({status:"Connected",provider});}catch(e){const reason=e instanceof Error?e.message:"Unknown provider error";await getSupabaseServerClient()?.from("system_logs").insert({level:"error",event:"provider_test_failed",metadata:{reason}});return NextResponse.json({status:"Failed",reason},{status:502});}}
