import { NextResponse } from "next/server"; import { getSupabaseServerClient } from "@/lib/supabase/server"; import { requireAdmin } from "@/lib/request-auth";
export async function GET(request: Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin(request);const{data,error}=await getSupabaseServerClient()!.from("distribution_jobs").select("*").eq("id",(await params).id).maybeSingle();if(error||!data)return NextResponse.json({error:"Not found"},{status:404});return NextResponse.json({job:data})}catch{return NextResponse.json({error:"Admin access required"},{status:403})}}
