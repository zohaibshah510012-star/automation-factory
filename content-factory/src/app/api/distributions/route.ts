import{NextResponse}from"next/server";import{createDistributionJob,processDistributionJob}from"@/lib/distribution-service";import{requireUser}from"@/lib/request-auth";export async function POST(r:Request){try{const u=await requireUser(r),b=await r.json(),j=await createDistributionJob({userId:u.id,contentId:b.contentId,contentType:b.contentType,platform:b.platform,payload:b.payload});void processDistributionJob(j.id);return NextResponse.json({job:j},{status:201})}catch{return NextResponse.json({error:"Unable to create distribution job"},{status:400})}}
