"use client";import{useEffect,useState}from"react";import{getSupabaseBrowserClient}from"@/lib/supabase/browser";
export default function Tasks(){const[d,setD]=useState<any[]>([]);useEffect(()=>{void(async()=>{const s=await getSupabaseBrowserClient()?.auth.getSession();const r=await fetch("/api/admin/overview",{headers:{Authorization:`Bearer ${s?.data.session?.access_token}`}});if(r.ok)setD((await r.json()).tasks);})();},[]);return <main className="mx-auto max-w-6xl p-6"><h1 className="text-3xl font-semibold">Task Monitor</h1><div className="mt-6 divide-y rounded-xl border">{d.map(x=><div className="p-3" key={x.id}>{x.id} · {x.status}</div>)}</div></main>}
