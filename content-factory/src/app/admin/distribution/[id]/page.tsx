"use client";import{useParams}from"next/navigation";import{useEffect,useState}from"react";import{getSupabaseBrowserClient}from"@/lib/supabase/browser";export default function Detail(){const{id}=useParams<{id:string}>(),[j,setJ]=useState<any>();useEffect(()=>{void(async()=>{const s=await getSupabaseBrowserClient()?.auth.getSession(),r=await fetch(`/api/admin/distributions/${id}`,{headers:{Authorization:`Bearer ${s?.data.session?.access_token??""}`}});if(r.ok)setJ((await r.json()).job)})()},[id]);return <main className="mx-auto max-w-4xl p-6"><h1 className="text-3xl font-semibold">发布任务</h1>{j?<pre className="mt-6 whitespace-pre-wrap rounded border p-4 text-sm">{JSON.stringify(j,null,2)}</pre>:<p className="mt-6">加载中…</p>}</main>}
