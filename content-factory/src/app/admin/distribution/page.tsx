"use client";import{useEffect,useState}from"react";import{getSupabaseBrowserClient}from"@/lib/supabase/browser";export default function Distribution(){const[j,setJ]=useState<any[]>([]);useEffect(()=>{void(async()=>{const s=await getSupabaseBrowserClient()?.auth.getSession(),r=await fetch("/api/admin/distributions",{headers:{Authorization:`Bearer ${s?.data.session?.access_token??""}`}});if(r.ok)setJ((await r.json()).jobs)})()},[]);return <main className="mx-auto max-w-6xl p-6"><h1 className="text-3xl font-semibold">内容分发</h1><div className="mt-6 divide-y rounded border">{j.map(x=><div className="p-3" key={x.id}>{x.platform} · {x.status} · {x.error??"—"}</div>)}</div></main>}
