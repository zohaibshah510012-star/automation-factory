"use client";import{useEffect,useState}from"react";import{Badge}from"@/components/ui/badge";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from"@/components/ui/table";import{getSupabaseBrowserClient}from"@/lib/supabase/browser";type Task={id:string;prompt:string;provider:string|null;status:string;error:string|null;createdAt:string};export default function AdminVideos(){const[tasks,setTasks]=useState<Task[]>([]);useEffect(()=>{void(async()=>{const s=await getSupabaseBrowserClient()?.auth.getSession();const r=await fetch("/api/admin/videos",{headers:{Authorization:`Bearer ${s?.data.session?.access_token??""}`}});if(r.ok)setTasks((await r.json()).tasks)})()},[]);return <main className="mx-auto max-w-6xl p-6"><header><p className="text-sm text-muted-foreground">Admin Console</p><h1 className="text-3xl font-semibold">视频任务监控</h1></header><Card className="mt-6"><CardHeader><CardTitle>最近视频任务</CardTitle></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>Prompt</TableHead><TableHead>Provider</TableHead><TableHead>状态</TableHead><TableHead>失败记录</TableHead><TableHead>时间</TableHead></TableRow></TableHeader><TableBody>{tasks.map(t=><TableRow key={t.id}><TableCell className="max-w-64 truncate">{t.prompt}</TableCell><TableCell>{t.provider??"—"}</TableCell><TableCell><Badge variant={t.status==="failed"?"destructive":"secondary"}>{t.status}</Badge></TableCell><TableCell className="max-w-64 truncate">{t.error??"—"}</TableCell><TableCell>{new Date(t.createdAt).toLocaleString("zh-CN")}</TableCell></TableRow>)}</TableBody></Table></CardContent></Card></main>}
