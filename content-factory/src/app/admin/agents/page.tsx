"use client";import{useEffect,useState}from"react";import{getSupabaseBrowserClient}from"@/lib/supabase/browser";
export default function Agents(){const[a,setA]=useState<any[]>([]);useEffect(()=>{void(async()=>{const s=await getSupabaseBrowserClient()?.auth.getSession();const r=await fetch("/api/admin/agents",{headers:{Authorization:`Bearer ${s?.data.session?.access_token}`}});if(r.ok)setA((await r.json()).data);})();},[]);return <main className="mx-auto max-w-6xl p-6"><h1 className="text-3xl font-semibold">Agents</h1><div className="mt-6 divide-y rounded-xl border">{a.map(x=><div className="p-4" key={x.id}><b>{x.agent_name}</b> · {x.provider_name} · {x.enabled?"enabled":"disabled"}</div>)}</div></main>}
