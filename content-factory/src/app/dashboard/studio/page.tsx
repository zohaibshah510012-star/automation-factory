"use client";import Link from"next/link";import{useEffect,useState}from"react";import{Badge}from"@/components/ui/badge";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{getSupabaseBrowserClient}from"@/lib/supabase/browser";type Drama={id:string;title:string;status:string;created_at:string;progress:{total:number;completed:number;failed:number}};export default function Studio(){const[d,setD]=useState<Drama[]>([]),[e,setE]=useState("");useEffect(()=>{void(async()=>{const s=await getSupabaseBrowserClient()?.auth.getSession(),r=await fetch("/api/dramas",{headers:{Authorization:`Bearer ${s?.data.session?.access_token??""}`}});if(r.ok)setD((await r.json()).dramas);else setE("无法加载短剧资产。")})()},[]);return <main className="mx-auto max-w-6xl p-6"><h1 className="text-3xl font-semibold">短剧工作室</h1>{e?<p className="mt-4 text-sm text-destructive">{e}</p>:null}<section className="mt-6 grid gap-4 md:grid-cols-2">{d.map(x=><Card key={x.id}><CardHeader><CardTitle>{x.title}</CardTitle></CardHeader><CardContent><Badge variant="secondary">{x.status}</Badge><p className="mt-3 text-sm text-muted-foreground">进度：{x.progress.completed}/{x.progress.total} · 失败 {x.progress.failed}</p><p className="mt-2 text-xs text-muted-foreground">{new Date(x.created_at).toLocaleString("zh-CN")}</p><Link className="mt-4 inline-block text-sm text-primary" href={`/dashboard/studio/${x.id}`}>查看短剧 →</Link></CardContent></Card>)}</section>{!d.length&&!e?<p className="mt-10 text-center text-sm text-muted-foreground">暂无短剧资产。</p>:null}</main>}
