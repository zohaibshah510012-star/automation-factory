"use client";

import { ImageIcon, RefreshCwIcon, SendIcon } from "lucide-react";
import { useEffect, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardAction, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { getSupabaseBrowserClient } from "@/lib/supabase/browser";

type ImageTask={id:string;prompt:string;provider:string|null;model:string|null;size:string|null;status:"pending"|"running"|"completed"|"failed";resultUrl:string|null;metadata:Record<string,unknown>;error:string|null;createdAt:string};
async function authHeaders(){const session=await getSupabaseBrowserClient()?.auth.getSession();return{Authorization:`Bearer ${session?.data.session?.access_token??""}`};}
export default function ImagesPage(){const[tasks,setTasks]=useState<ImageTask[]>([]);const[prompt,setPrompt]=useState("");const[loading,setLoading]=useState(false);const[message,setMessage]=useState("");async function load(){const response=await fetch("/api/images",{headers:await authHeaders(),cache:"no-store"});if(response.ok)setTasks((await response.json()).tasks);}useEffect(()=>{void load();},[]);async function create(){if(!prompt.trim())return;setLoading(true);setMessage("");const response=await fetch("/api/images",{method:"POST",headers:{...(await authHeaders()),"Content-Type":"application/json"},body:JSON.stringify({prompt})});if(response.ok){setPrompt("");setMessage("图片任务已创建，正在生成。");setTimeout(()=>void load(),800);}else setMessage((await response.json()).error??"图片任务创建失败");setLoading(false);}async function regenerate(id:string){const response=await fetch(`/api/images/${id}/regenerate`,{method:"POST",headers:await authHeaders()});if(response.ok){setMessage("已创建新的图片任务。");setTimeout(()=>void load(),800);}else setMessage("重新生成失败");}return <main className="mx-auto flex max-w-6xl flex-col gap-6 p-6"><header><p className="text-sm text-muted-foreground">Multimodal Content Engine</p><h1 className="text-3xl font-semibold">图片生成</h1><p className="mt-2 text-sm text-muted-foreground">输入图片描述，任务会使用已启用的图片 Provider 生成并保存结果。</p></header><Card><CardHeader><CardTitle>创建图片任务</CardTitle><CardDescription>Provider 由服务器环境与后台配置决定，密钥不会暴露到浏览器。</CardDescription></CardHeader><CardContent className="flex flex-col gap-3 sm:flex-row"><Input value={prompt} onChange={(event)=>setPrompt(event.target.value)} placeholder="例如：极简科技品牌的产品海报，深色背景，电影级灯光"/><Button disabled={loading||!prompt.trim()} onClick={()=>void create()}><SendIcon data-icon="inline-start"/>{loading?"创建中…":"生成图片"}</Button></CardContent></Card>{message?<p className="text-sm text-muted-foreground">{message}</p>:null}<section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{tasks.map(task=><Card key={task.id}><CardHeader><CardTitle className="line-clamp-2">{task.prompt}</CardTitle><CardDescription>{new Date(task.createdAt).toLocaleString("zh-CN")}</CardDescription><CardAction><Badge variant={task.status==="failed"?"destructive":"secondary"}>{task.status}</Badge></CardAction></CardHeader><CardContent className="flex flex-col gap-3">{task.resultUrl&& !task.resultUrl.startsWith("mock://")?<img className="aspect-square rounded-lg object-cover" src={task.resultUrl} alt={task.prompt}/>:<div className="flex aspect-square items-center justify-center rounded-lg bg-muted text-sm text-muted-foreground"><ImageIcon className="mr-2 size-4"/>{task.status==="completed"?"图片结果已保存":"等待图片结果"}</div>}<div className="flex flex-col gap-1 text-xs text-muted-foreground"><span>{task.provider??"等待 Provider"}{task.model?` · ${task.model}`:""}</span>{task.error?<span className="text-destructive">{task.error}</span>:null}</div><Button size="sm" variant="outline" onClick={()=>void regenerate(task.id)}><RefreshCwIcon data-icon="inline-start"/>重新生成</Button></CardContent></Card>)}</section>{!tasks.length?<Card><CardContent className="py-12 text-center text-muted-foreground">还没有图片任务。</CardContent></Card>:null}</main>}
