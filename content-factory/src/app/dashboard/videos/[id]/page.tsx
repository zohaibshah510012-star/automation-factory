"use client";
import Link from"next/link";import{useEffect,useState}from"react";import{useParams}from"next/navigation";import{RefreshCwIcon}from"lucide-react";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{getSupabaseBrowserClient}from"@/lib/supabase/browser";
type Task={id:string;prompt:string;provider:string|null;model:string|null;status:string;videoUrl:string|null;thumbnailUrl:string|null;error:string|null;createdAt:string};async function headers(){const s=await getSupabaseBrowserClient()?.auth.getSession();return{Authorization:`Bearer ${s?.data.session?.access_token??""}`};}export default function VideoDetail(){const{id}=useParams<{id:string}>(),[task,setTask]=useState<Task>();useEffect(()=>{void(async()=>{const r=await fetch(`/api/videos/${id}`,{headers:await headers()});if(r.ok)setTask((await r.json()).task)})()},[id]);const regen=async()=>{await fetch(`/api/videos/${id}/regenerate`,{method:"POST",headers:await headers()})};if(!task)return <main className="p-6 text-sm text-muted-foreground">正在加载视频任务…</main>;return <main className="mx-auto flex max-w-4xl flex-col gap-6 p-6"><Link className="text-sm text-primary" href="/dashboard/videos">← 返回视频列表</Link><header><Badge variant="secondary">{task.status}</Badge><h1 className="mt-3 text-3xl font-semibold">视频任务</h1><p className="mt-2 text-muted-foreground">{task.prompt}</p></header><Card><CardHeader><CardTitle>视频结果</CardTitle></CardHeader><CardContent>{task.videoUrl&&!task.videoUrl.startsWith("mock://")?<video className="w-full rounded-lg" controls poster={task.thumbnailUrl??undefined} src={task.videoUrl}/>:<div className="flex aspect-video items-center justify-center rounded-lg bg-muted text-muted-foreground">{task.status==="completed"?"视频已保存":"视频生成中"}</div>}<p className="mt-4 text-sm text-muted-foreground">{task.provider??"—"}{task.model?` · ${task.model}`:""}</p>{task.error?<p className="mt-2 text-sm text-destructive">{task.error}</p>:null}<Button className="mt-4" variant="outline" onClick={()=>void regen()}><RefreshCwIcon data-icon="inline-start"/>重新生成</Button></CardContent></Card></main>}
